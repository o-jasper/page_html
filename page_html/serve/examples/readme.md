Some basic examples. `init.lua` loads them all.

* `direct.lua` just writes out html

* `templated.lua` the 

* `basic_dir_explorer.lua`  *basic* directory viewer using 
  [lfs](https://keplerproject.github.io/luafilesystem/manual.html).
